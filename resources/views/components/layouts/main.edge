<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Manajemen Toko</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen bg-gray-100 text-gray-900">
    <nav class="bg-white shadow p-4 flex justify-between items-center">
  <h1 class="text-xl font-bold">Manajemen Toko</h1>
  <div class="flex gap-4 items-center">
    <a href="/dashboard" class="text-blue-600 hover:underline">Dashboard</a>
    <a href="/kategori" class="text-blue-600 hover:underline">Kategori</a>
    <a href="/produk" class="text-blue-600 hover:underline">Produk</a>
    <a href="/supplier" class="text-blue-600 hover:underline">Supplier</a>
    <a href="/penjualan" class="text-blue-600 hover:underline">Penjualan</a>
    <a href="/stok" class="text-blue-600 hover:underline">Stok</a>

    @if(auth.user)
      <form method="POST" action="/logout" class="inline">
        <input type="hidden" name="_csrf" value="{{ csrfToken }}">
        <button type="submit" class="text-red-600 hover:underline">Logout</button>
      </form>
    @else
      <a href="/login" class="text-blue-600 hover:underline">Login</a>
      <a href="/register" class="text-blue-600 hover:underline">Register</a>
    @endif
  </div>
</nav>

    </nav>
    

    <main class="p-8">
      {{{ await $slots.main() }}}
    </main>
  </body>
</html>
